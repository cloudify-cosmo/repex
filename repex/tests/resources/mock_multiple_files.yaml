variables:
    version: 1.1m2

paths:
    -   type: mock_VERSION
        path: repex/tests/resources/
        base_directory: repex/tests/resources/
        replace: "{{ .preversion }}"
        with: "{{ .version }}"
        validate_before: true
        must_include:
            - date
            - commit
            - version